import React from 'react';

const Instructions = (props) => (
    <div>
        <p className="important-text">Choose your options below. To start the test, begin typing.</p>
    </div>
);

export default Instructions;